<resources>

    <!-- Base application theme. -->
    <style name="AppTheme" parent="Theme.AppCompat.DayNight.NoActionBar">
        <item name="windowActionBar">false</item>
        <item name="android:windowNoTitle">true</item>

        <!-- Customize your theme here. -->
        <item name="colorPrimary">@color/colorPrimary</item>
        <item name="colorPrimaryDark">@color/colorPrimaryDark</item>
        <item name="colorAccent">@color/colorAccent</item>
    </style>

    <style name="RxRankTheme" parent="@style/AppTheme">
        <item name="toolbarColor">@color/blue</item>
        <item name="textColorLight">@color/white</item>
        <item name="textColorDark" >@color/black</item>
        <item name="listItemColor">@color/white</item>
        <item name="backgroundColor">@color/white</item>
        <!--item name="android:windowBackground">@color/white</item-->
        <item name="drawer_hp_color">@color/white_dark</item>
        <item name="textColor_dr_lv">@color/black</item>
        <item name="recycleViewColor">@color/white_gray</item>
        <item name="cardViewColor">@color/white</item>

        <item name="tab_layout_color_home">@color/blue</item>
        <item name="tab_layout_text_color">@color/white</item>
        <item name="tab_indicator_color">@color/white</item>
        <item name="tab_layout_selected_color">@color/white</item>
        
        <item name="drawerArrowStyle">@style/MyDrawerStyle</item>
    </style>


    <style name="TabLayout_Text_Appearance" parent="TextAppearance.Design.Tab">
        <!--禁止自动大写 -->
        <item name="textAllCaps">false</item>
        <item name="android:textStyle">bold</item>
    </style>
    
    <style name="MyDrawerStyle" parent="Widget.AppCompat.DrawerArrowToggle">
        <!-- navigation icon 设置旋转效果-->
        <item name="spinBars">true</item>
        <!-- navigation icon 的颜色-->
        <item name="color">@color/drawer_icon_color</item>
    </style>
</resources>
